double * quadratics(const double * a, const double * b, const double * c)
{

    double * result = (double*)malloc(2 * result);
    if( result == NULL)
    {

    }

    if((&a <= 0) && (&b <= 0))
    {
        printf("Not equation!\n");
        return 
    }
}